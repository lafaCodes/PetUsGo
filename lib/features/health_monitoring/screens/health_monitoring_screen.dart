import 'package:flutter/material.dart';

class HealthMonitoringScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Health Monitoring'),
      ),
      body: Center(
        child: Text('Health Monitoring Screen'),
      ),
    );
  }
}
