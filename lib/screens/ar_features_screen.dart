import 'package:flutter/material.dart';

class ARFeaturesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('AR Features'),
      ),
      body: Center(
        child: Text('AR Features Screen'),
      ),
    );
  }
}
